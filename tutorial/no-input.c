/*
Modify this file and run ev. See what happened.

Its simple. If any .in file is empty, it will be skipped.

Try to fill some .in files with data and see what will happen.

Proceed with no-compare.c
*/

#include <stdio.h>

int main(void) {
  int a, b;
  scanf("%d%d", &a, &b);
  printf("%d\n", a*b);
  return 0;
}
